// CONTROLLER - Jogadores (pode ser expandido futuramente)
const jogadoresController = {
    // Funções para manipular jogadores podem ser adicionadas aqui
    init: function() {
        console.log('Jogadores Controller inicializado');
    }
};

// Inicializar se necessário
document.addEventListener('DOMContentLoaded', () => {
    jogadoresController.init();
});